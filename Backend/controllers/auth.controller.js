export const signUp = (req, res) => {
  res.json({
    data: "Signup end point"
  })
}

export const logIn = (req, res) => {
  res.json({
    data: "Login end point"
  })
}

export const logOut = (req, res) => {
  res.json({
    data: "Logout end point"
  })
}
